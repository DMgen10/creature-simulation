package org.simulation.action;

import org.simulation.core.Board;

public class GrassSpawn implements Action{

    @Override
    public void execute(Board board) {
        for (int col = 0; col < ; col++) {
            
        }
    }
}
